import 'package:flutter/material.dart';

class GunBuddiesPage extends StatelessWidget {
  const GunBuddiesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Gun Buddies'),
    );
  }
}